applications:
 - name: examcollab
   path: target/examcollab.war
   instances: 0
   memory: 0M #remettre 256 une fois le TP JPA terminé
   disk_quota: 0M #remettre 256 une fois le TP JPA terminé
   services:
   - sqldatabase
